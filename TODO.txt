X Mocking library - needed for Status at first
X Blocking listener
X  Stats
X Non blocking listener
X Rename *Test to *Spec
X Individual stats implementations
X Reporting
X Fail if JVM encoding not UTF-8
X Cleanup pom and imports
X Handle errors in futures (and other places?)
X Unit tests for listeners / other missing tests

rename metrics
emojis -> emoji
In perf test, tps not used from CurrentStats
Add assertions for times in tests  rf
Do we really want the type alias?
  Can we do something better than the map?
Can we remove mutability (CurrentStats)?  This is probably not safe with parallel tests.
? Handle emoji char ranges
